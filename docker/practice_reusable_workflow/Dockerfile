# 軽量なベースイメージ
FROM alpine:3.20

# メタ情報（任意）
LABEL maintainer="practice"
LABEL description="Reusable workflow practice image"

# 簡単なコマンドを実行
RUN echo "Building practice container..."

# コンテナ起動時のデフォルト動作
CMD ["echo", "Hello from reusable workflow practice container"]